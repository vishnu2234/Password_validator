# Password_validator
A simple console-based Password Validator built using Java. It validates user-entered passwords based on basic security rules and ensures strong password creation.

#Features
Check minimum length (at least 8 characters)
Validate presence of uppercase letters
Validate presence of digits
Display specific error messages for invalid inputs
Continuous input until a valid password is entered

#Technologies
Core Java 
String handling
Scanner for user input
